import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Client-side rendering only - no SSR
  // All API calls go to backend REST API
};

export default nextConfig;
